import css from 'styled-jsx/css'

export default css`

.contact-block {
    margin-bottom: 20px;
}

a.contact-btn, a.contact-btn:hover, a.contact-btn:visited {
    color: inherit;
    text-decoration: inherit;
}

`
